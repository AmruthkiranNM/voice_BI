# ============================================================
# Voice BI — Python Dependencies
# ============================================================

# Web framework
fastapi==0.115.0
uvicorn[standard]==0.30.0
pydantic==2.9.0
python-multipart==0.0.9

# Data processing
pandas>=2.0.0

# Embeddings
# torch is pulled in by sentence-transformers; pin the CPU-only build so
# pip doesn't grab a multi-GB CUDA wheel. Embeddings run on CPU (see
# config.EMBEDDING_DEVICE) so the local GPU's VRAM stays free for Ollama.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.4.1+cpu
sentence-transformers==3.0.0

# Vector store
faiss-cpu>=1.12.0

# Utilities
numpy>=1.26.0
python-dotenv==1.0.1

# Testing
pytest>=8.0.0
httpx>=0.27.0
